# Force binary wheels to avoid compilation
--only-binary=:all:

# Core FastAPI stack (no compilation needed)
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0
starlette==0.27.0

# Database (PostgreSQL binary wheel)
sqlalchemy==2.0.23
asyncpg==0.29.0
alembic==1.13.1
psycopg2-binary==2.9.9

# Authentication (pure Python/binary wheels)
passlib==1.7.4
python-jose==3.3.0
bcrypt==4.1.2

# Minimal ML stack (CPU-only, binary wheels)
numpy==1.24.4
nltk==3.8.1
textblob==0.17.1

# Core utilities (pure Python)
python-dotenv==1.0.0
requests==2.31.0
click==8.1.7
Jinja2==3.1.2

# Essential dependencies
typing-extensions==4.8.0
packaging==23.2
certifi==2023.11.17
charset-normalizer==3.3.2
idna==3.6
urllib3==2.1.0
